bases:
  - ../base
patchesJson6902:
  - target:
      group: rbac.authorization.k8s.io
      version: v1
      kind: ClusterRoleBinding
      name: cluster
    path: self-provisioners-clusterrolebinding.yaml
